# Copy to .env.local for local overrides

# Production only: URL of the deployed Socket.IO server (Render, Railway, etc.)
# VITE_SOCKET_URL=https://your-sync-server.onrender.com

# Set on the sync server (server/index.js):
# CLIENT_ORIGIN=https://your-app.vercel.app,http://localhost:5173
